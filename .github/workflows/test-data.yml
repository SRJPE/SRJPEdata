name: Data Quality and Package Checks

# Runs purely as a check against what's already been committed to this PR
# branch - it does NOT regenerate data or run QC/stream-team-review itself.
# update-data.yml does that (data pull + QC/review render + one commit).
#
# This is a `workflow_call` reusable workflow, not its own PR trigger -
# check-commit.yml calls it directly as a job right after its `update-data`
# job, in the SAME triggered run. It used to have its own `pull_request`
# trigger (path-filtered), relying on update-data.yml's commit landing back
# on the PR branch to fire a second, fresh `synchronize` run - but that
# second run is a *new* triggered workflow run, and pushes authored by the
# default GITHUB_TOKEN (which is what update-data.yml's commit step uses)
# can require manual "Approve and run workflows" depending on repo/org
# Actions settings, plus it redundantly re-ran lint-commit-messages and
# update-data.yml a second time (a no-op, but still a wasted DB pull).
# Chaining this as a job instead of a second event means everything -
# lint, data pull + QC/review + commit, then this comparison - happens in
# one PR-triggered run, in the right order, with no extra approval click
# and no redundant re-run.
#
# NOTE: this job's check now posts to the PR under the context
# "PR Data Update (check-commit -> update-data) / quality-checks" (check-
# commit.yml's workflow name + this job's id) instead of the old
# "Data Quality and Package Checks / quality-checks". If main's branch
# protection lists the old name as a required status check, update it to
# the new one or the PR will never show as mergeable.

on:
  workflow_call:
    inputs:
      ref:
        description: "PR branch to check out and compare against main."
        required: true
        type: string

jobs:
  quality-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ inputs.ref }}

      - name: Fetch main branch
        run: |
          git fetch origin main:main
          echo "Current branch: $(git branch --show-current)"

      - name: Resolve update-type from the last commit
        id: update-type
        run: |
          body="$(git log -1 --format=%B HEAD)"
          if echo "$body" | grep -qiE '^update-type:[[:space:]]*annual[[:space:]]*$'; then
            update_type="annual"
          elif echo "$body" | grep -qiE '^update-type:[[:space:]]*biweekly[[:space:]]*$'; then
            update_type="biweekly"
          else
            update_type="manual"
          fi
          echo "Resolved update_type=${update_type}"
          echo "update_type=${update_type}" >> "$GITHUB_OUTPUT"

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.3.0'
          use-public-rspm: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libxml2-dev libssl-dev libfontconfig1-dev libharfbuzz-dev libfribidi-dev libfreetype6-dev libpng-dev libtiff5-dev libjpeg-dev

      - name: Install package + all Imports/Suggests/Remotes from DESCRIPTION
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::devtools
            any::yaml
            any::remotes
            any::roxygen2

      # ==========================================
      # CREDENTIAL SCANNING
      # ==========================================

      - name: Check for credentials and sensitive files
        id: credential-check
        run: |
          echo "=== Checking for credentials and sensitive files ==="
          FOUND_ISSUES=0

          FORBIDDEN_FILES=(
            ".Renviron"
            ".env"
            ".httr-oauth"
            ".secrets"
            "credentials.R"
            "config.yml"
          )

          FORBIDDEN_PATTERNS=(
            "password\\s*=\\s*['\"][^'\"]{3,}['\"]"
            "api[_-]?key\\s*=\\s*['\"][^'\"]{10,}['\"]"
            "secret\\s*=\\s*['\"][^'\"]{10,}['\"]"
            "token\\s*=\\s*['\"][^'\"]{10,}['\"]"
            "aws_access_key_id"
            "aws_secret_access_key"
          )

          echo "Checking for forbidden files..."
          for file in "${FORBIDDEN_FILES[@]}"; do
            if git ls-files | grep -q "^${file}$"; then
              echo "❌ ERROR: Found forbidden file: $file"
              FOUND_ISSUES=$((FOUND_ISSUES + 1))
            fi
          done

          echo ""
          echo "Checking for credential patterns in code..."
          for pattern in "${FORBIDDEN_PATTERNS[@]}"; do
            matches=$(git grep -i -E "$pattern" -- '*.R' '*.r' '*.Rmd' '*.rmd' 2>/dev/null || true)
            if [ ! -z "$matches" ]; then
              echo "⚠️  WARNING: Found potential credential pattern:"
              echo "$matches"
              FOUND_ISSUES=$((FOUND_ISSUES + 1))
            fi
          done

          echo ""
          if [ $FOUND_ISSUES -eq 0 ]; then
            echo "✅ No credentials or sensitive files found"
          else
            echo "❌ Found $FOUND_ISSUES potential issues"
          fi

          echo "credential_issues=$FOUND_ISSUES" >> $GITHUB_OUTPUT

      # ==========================================
      # R PACKAGE STRUCTURE CHECK
      # ==========================================

      - name: Check R package structure
        id: structure-check
        run: |
          echo "=== Checking R package structure ==="
          STRUCTURE_ISSUES=0

          REQUIRED_FILES=(
            "DESCRIPTION"
            "NAMESPACE"
          )

          REQUIRED_DIRS=(
            "R"
            "man"
          )

          echo "Checking required files..."
          for file in "${REQUIRED_FILES[@]}"; do
            if [ ! -f "$file" ]; then
              echo "❌ ERROR: Missing required file: $file"
              STRUCTURE_ISSUES=$((STRUCTURE_ISSUES + 1))
            else
              echo "✓ Found: $file"
            fi
          done

          echo ""
          echo "Checking required directories..."
          for dir in "${REQUIRED_DIRS[@]}"; do
            if [ ! -d "$dir" ]; then
              echo "❌ ERROR: Missing required directory: $dir/"
              STRUCTURE_ISSUES=$((STRUCTURE_ISSUES + 1))
            else
              echo "✓ Found: $dir/"
            fi
          done

          echo ""
          echo "Checking DESCRIPTION file format..."
          if [ -f "DESCRIPTION" ]; then
            if ! grep -q "^Package:" DESCRIPTION; then
              echo "❌ ERROR: DESCRIPTION missing 'Package:' field"
              STRUCTURE_ISSUES=$((STRUCTURE_ISSUES + 1))
            fi
            if ! grep -q "^Version:" DESCRIPTION; then
              echo "❌ ERROR: DESCRIPTION missing 'Version:' field"
              STRUCTURE_ISSUES=$((STRUCTURE_ISSUES + 1))
            fi
          fi

          echo ""
          if [ $STRUCTURE_ISSUES -eq 0 ]; then
            echo "✅ Package structure looks good"
          else
            echo "❌ Found $STRUCTURE_ISSUES structure issues"
          fi

          echo "structure_issues=$STRUCTURE_ISSUES" >> $GITHUB_OUTPUT

      # ==========================================
      # TESTTHAT TESTS
      # ==========================================

      - name: Run testthat tests
        id: testthat
        continue-on-error: true
        run: |
          echo "=== Running testthat tests ==="

          if [ ! -d "tests/testthat" ]; then
            echo "⚠️  No tests/testthat directory found"
            echo "test_status=skipped" >> $GITHUB_OUTPUT
            exit 0
          fi

          TEST_FILES=$(find tests/testthat -name "test*.R" | wc -l)
          echo "Found $TEST_FILES test file(s)"

          if [ $TEST_FILES -eq 0 ]; then
            echo "⚠️  No test files found in tests/testthat/"
            echo "test_status=skipped" >> $GITHUB_OUTPUT
            exit 0
          fi

          # GitHub Actions runs this `run:` block as `bash -e` (errexit), and
          # the Rscript call below is expected to exit non-zero whenever
          # tests fail (that's how it reports failure). Under errexit, a
          # plain non-zero-exiting command on its own line kills the whole
          # script immediately - so without disabling it here, a real test
          # failure would abort the step right after the Rscript call and
          # BEFORE "TEST_EXIT_CODE=$?"/the echo below ever ran, leaving
          # test_status unset. That's the actual cause of the "❓ Status
          # unknown" the PR comment shows: it only happened when tests
          # failed, never when they passed, because errexit only aborts on
          # the non-zero case. set +e / set -e around just this call fixes
          # it without changing errexit behavior for the rest of the step.
          set +e
          Rscript -e "
          library(testthat)

          tryCatch({
            if (requireNamespace('devtools', quietly = TRUE)) {
              devtools::load_all('.', quiet = TRUE)
              cat('✓ Package loaded with devtools\n')
            } else {
              cat('⚠️  devtools not available, loading R files directly\n')
              r_files <- list.files('R', pattern = '\\\\.R$', full.names = TRUE)
              lapply(r_files, source)
              cat('✓ Sourced', length(r_files), 'R file(s)\n')
            }
          }, error = function(e) {
            cat('⚠️  Warning: Could not load package:', conditionMessage(e), '\n')
          })

          cat('\nRunning tests...\n')
          results <- test_dir('tests/testthat', reporter = 'summary')

          # Per-test-block counts (one row per test_that() call), captured so
          # the PR comment can show real numbers instead of just pass/fail -
          # this comes for free from the same run, no second R invocation.
          n_total   <- nrow(results)
          n_failed  <- sum(results\$failed > 0)
          n_skipped <- sum(results\$skipped > 0)
          n_warned  <- sum(results\$warning > 0)
          n_passed  <- n_total - n_failed - n_skipped

          gh_out <- Sys.getenv('GITHUB_OUTPUT')
          cat(sprintf('test_total=%d\n',   n_total),   file = gh_out, append = TRUE)
          cat(sprintf('test_passed=%d\n',  n_passed),  file = gh_out, append = TRUE)
          cat(sprintf('test_failed=%d\n',  n_failed),  file = gh_out, append = TRUE)
          cat(sprintf('test_skipped=%d\n', n_skipped), file = gh_out, append = TRUE)
          cat(sprintf('test_warned=%d\n',  n_warned),  file = gh_out, append = TRUE)

          cat(sprintf('\nTest summary: %d total | %d passed | %d failed | %d skipped | %d with warnings\n',
                      n_total, n_passed, n_failed, n_skipped, n_warned))

          if (any(results\$failed > 0)) {
            cat('\n❌ Some tests failed\n')
            quit(status = 1)
          } else if (any(results\$warning > 0)) {
            cat('\n⚠️  Tests passed with warnings\n')
            quit(status = 0)
          } else {
            cat('\n✅ All tests passed\n')
            quit(status = 0)
          }
          "

          TEST_EXIT_CODE=$?
          set -e

          if [ $TEST_EXIT_CODE -eq 0 ]; then
            echo "test_status=passed" >> $GITHUB_OUTPUT
          else
            echo "test_status=failed" >> $GITHUB_OUTPUT
          fi

          exit $TEST_EXIT_CODE

      # ==========================================
      # DATA COMPARISON (vs main)
      # ==========================================

      - name: Get changed RDA files
        id: changed-files
        uses: tj-actions/changed-files@v46
        with:
          files: |
            data/*.rda
            data/*.RData

      - name: List all data files
        id: all-data-files
        run: |
          ALL=$(find data -name "*.rda" -o -name "*.RData" 2>/dev/null | sort | tr '\n' ' ')
          echo "all_files=${ALL}" >> $GITHUB_OUTPUT

      - name: Compare data files
        if: steps.changed-files.outputs.any_changed == 'true'
        id: comparison
        continue-on-error: true
        run: |
          Rscript inst/scripts/compare_data_files.R "${{ steps.changed-files.outputs.all_changed_files }}"
          echo "comparison_exit_code=$?" >> $GITHUB_OUTPUT

      - name: Upload comparison report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: data-comparison-report
          path: comparison_report.txt
          retention-days: 30

      # ==========================================
      # QC LOG COMPARISON (vs main)
      # ==========================================
      # update-data.yml already ran data-raw/qc/run_annual_qc.R and
      # data-raw/data-checks/stream_team_review/render_reviews.R and
      # committed their outputs to this branch (unless update_type was
      # biweekly). This step doesn't re-render anything - it just diffs
      # this branch's committed data-raw/qc/qc_log_errors.csv against
      # main's copy, the same "compare against the other branch" pattern
      # as the .rda comparison above.
      #
      # The QC log is a running, human-annotated backlog, not a clean
      # pass/fail signal - qc_log_errors.csv already has open issues in it
      # on main today. So this only fails on error-level issues that are
      # OPEN on this branch but were NOT already open on main - i.e. new
      # issues this update introduced, not the pre-existing backlog (which
      # is still surfaced, just non-blocking, in the PR comment below).

      - name: Compare QC log against main
        if: steps.update-type.outputs.update_type != 'biweekly'
        id: qc-comparison
        continue-on-error: true
        run: |
          Rscript - <<'RSCRIPT'
          read_open_ids <- function(lines) {
            if (length(lines) == 0) return(character(0))
            con <- textConnection(lines)
            df <- tryCatch(
              readr::read_csv(con, show_col_types = FALSE,
                               col_types = readr::cols(.default = "c")),
              error = function(e) NULL
            )
            close(con)
            if (is.null(df) || nrow(df) == 0) return(character(0))
            df <- df[rowSums(!is.na(df)) > 0, ]
            if (nrow(df) == 0) return(character(0))
            df$log_id[df$status == "open"]
          }

          errors_path   <- "data-raw/qc/qc_log_errors.csv"
          warnings_path <- "data-raw/qc/qc_log_warnings.csv"

          this_branch_errors   <- if (file.exists(errors_path)) readLines(errors_path) else character(0)
          this_branch_warnings <- if (file.exists(warnings_path)) readLines(warnings_path) else character(0)

          main_errors <- suppressWarnings(
            system2("git", c("show", "main:data-raw/qc/qc_log_errors.csv"),
                    stdout = TRUE, stderr = FALSE)
          )
          main_status <- attr(main_errors, "status")
          if (!is.null(main_status) && main_status != 0) main_errors <- character(0)

          main_warnings <- suppressWarnings(
            system2("git", c("show", "main:data-raw/qc/qc_log_warnings.csv"),
                    stdout = TRUE, stderr = FALSE)
          )
          main_warnings_status <- attr(main_warnings, "status")
          if (!is.null(main_warnings_status) && main_warnings_status != 0) main_warnings <- character(0)

          open_errors_this_branch   <- read_open_ids(this_branch_errors)
          open_errors_main          <- read_open_ids(main_errors)
          open_warnings_this_branch <- read_open_ids(this_branch_warnings)
          open_warnings_main        <- read_open_ids(main_warnings)

          new_open_errors   <- setdiff(open_errors_this_branch, open_errors_main)
          new_open_warnings <- setdiff(open_warnings_this_branch, open_warnings_main)

          gh_out <- Sys.getenv("GITHUB_OUTPUT")
          cat(sprintf("new_open_errors=%d\n",     length(new_open_errors)),           file = gh_out, append = TRUE)
          cat(sprintf("total_open_errors=%d\n",   length(open_errors_this_branch)),   file = gh_out, append = TRUE)
          cat(sprintf("new_open_warnings=%d\n",   length(new_open_warnings)),         file = gh_out, append = TRUE)
          cat(sprintf("total_open_warnings=%d\n", length(open_warnings_this_branch)), file = gh_out, append = TRUE)

          cat(sprintf(
            "This branch open errors: %d | main open errors: %d | new vs main: %d\n",
            length(open_errors_this_branch), length(open_errors_main), length(new_open_errors)
          ))
          if (length(new_open_errors) > 0) {
            cat("\nOpen error log_ids on this branch not already open on main:\n")
            print(new_open_errors)
          }
          cat(sprintf(
            "This branch open warnings: %d | main open warnings: %d | new vs main: %d\n",
            length(open_warnings_this_branch), length(open_warnings_main), length(new_open_warnings)
          ))
          if (length(new_open_warnings) > 0) {
            cat("\nOpen warning log_ids on this branch not already open on main:\n")
            print(new_open_warnings)
          }
          RSCRIPT

      # ==========================================
      # REPORTING
      # ==========================================

      - name: Comment PR with results
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            const credentialIssues = parseInt('${{ steps.credential-check.outputs.credential_issues }}') || 0;
            const structureIssues = parseInt('${{ steps.structure-check.outputs.structure_issues }}') || 0;
            const testStatus = '${{ steps.testthat.outputs.test_status }}' || 'unknown';
            const testTotalRaw = parseInt('${{ steps.testthat.outputs.test_total }}');
            const hasTestCounts = !isNaN(testTotalRaw);
            const testTotal = hasTestCounts ? testTotalRaw : 0;
            const testPassed = parseInt('${{ steps.testthat.outputs.test_passed }}') || 0;
            const testFailed = parseInt('${{ steps.testthat.outputs.test_failed }}') || 0;
            const testSkipped = parseInt('${{ steps.testthat.outputs.test_skipped }}') || 0;
            const testWarned = parseInt('${{ steps.testthat.outputs.test_warned }}') || 0;
            const comparisonExitCode = '${{ steps.comparison.outputs.comparison_exit_code }}';
            const updateType = '${{ steps.update-type.outputs.update_type }}' || 'manual';
            const qcComparisonOutcome = '${{ steps.qc-comparison.outcome }}';
            const newOpenErrors = parseInt('${{ steps.qc-comparison.outputs.new_open_errors }}') || 0;
            const totalOpenErrors = parseInt('${{ steps.qc-comparison.outputs.total_open_errors }}') || 0;
            const newOpenWarnings = parseInt('${{ steps.qc-comparison.outputs.new_open_warnings }}') || 0;
            const totalOpenWarnings = parseInt('${{ steps.qc-comparison.outputs.total_open_warnings }}') || 0;

            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const branch = '${{ inputs.ref }}';
            // htmlpreview.github.io renders a raw GitHub file as a live page instead
            // of GitHub's own blob view, which just shows HTML files as source text.
            const previewLink = (path) => `https://htmlpreview.github.io/?https://raw.githubusercontent.com/${owner}/${repo}/${branch}/${path}`;

            let comparisonReport = 'No comparison report generated.';
            if (fs.existsSync('comparison_report.txt')) {
              comparisonReport = fs.readFileSync('comparison_report.txt', 'utf8');
            }

            const allDataFiles = '${{ steps.all-data-files.outputs.all_files }}'.trim().split(/\s+/).filter(Boolean);
            const changedDataFiles = '${{ steps.changed-files.outputs.all_changed_files }}'.trim().split(/\s+/).filter(Boolean);
            const unchangedDataFiles = allDataFiles.filter(f => !changedDataFiles.includes(f));

            let statusSummary = '## 🔍 Quality Check Summary\n\n';
            statusSummary += `_update-type: \`${updateType}\`_\n\n`;

            if (credentialIssues > 0) {
              statusSummary += '❌ **Credential Scan:** Found ' + credentialIssues + ' potential issue(s)\n';
            } else {
              statusSummary += '✅ **Credential Scan:** No credentials detected\n';
            }

            if (structureIssues > 0) {
              statusSummary += '❌ **Package Structure:** Found ' + structureIssues + ' issue(s)\n';
            } else {
              statusSummary += '✅ **Package Structure:** Valid R package structure\n';
            }

            if (testStatus === 'passed') {
              statusSummary += hasTestCounts
                ? `✅ **Tests:** ${testPassed} passed, ${testFailed} failed, ${testSkipped} skipped (${testTotal} total)${testWarned > 0 ? `, ${testWarned} with warnings` : ''}\n`
                : '✅ **Tests:** All tests passed\n';
            } else if (testStatus === 'failed') {
              statusSummary += hasTestCounts
                ? `❌ **Tests:** ${testFailed} failed, ${testPassed} passed, ${testSkipped} skipped (${testTotal} total)\n`
                : '❌ **Tests:** Some tests failed\n';
            } else if (testStatus === 'skipped') {
              statusSummary += '⚠️ **Tests:** No tests found (skipped)\n';
            } else {
              statusSummary += '❓ **Tests:** Status unknown\n';
            }

            if (changedDataFiles.length === 0) {
              statusSummary += 'ℹ️ **Data Comparison (vs main):** No data files changed in this PR\n';
            } else {
              statusSummary += `ℹ️ **Data Comparison (vs main):** ${changedDataFiles.length} file(s) changed — see the Data File Comparison section below (informational only, does not block this PR)\n`;
            }

            if (updateType === 'biweekly') {
              statusSummary += '⏭️ **QC Log (vs main):** skipped (biweekly update)\n';
            } else if (qcComparisonOutcome === 'success' || qcComparisonOutcome === 'failure') {
              const existingErrors = Math.max(totalOpenErrors - newOpenErrors, 0);
              const existingWarnings = Math.max(totalOpenWarnings - newOpenWarnings, 0);
              const qcIcon = newOpenErrors > 0 ? '❌' : '✅';
              statusSummary += `${qcIcon} **QC Log (vs main):** ${newOpenErrors} new open error(s) (${existingErrors} existing) · ${newOpenWarnings} new open warning(s) (${existingWarnings} existing)\n`;
              statusSummary += `   _(only new open errors block this PR - the existing backlog and all warnings are informational)_\n`;
            } else {
              statusSummary += '❓ **QC Log (vs main):** comparison did not run\n';
            }

            statusSummary += '\n---\n\n';

            let detailedReport = '';

            let dataSection = '';

            if (changedDataFiles.length > 0) {
              dataSection = `### 📊 Data File Comparison (vs main)\n\n${comparisonReport}`;
            }

            if (unchangedDataFiles.length > 0) {
              const unchangedList = unchangedDataFiles.map(f => `\`${f}\``).join(', ');
              dataSection += `\n\n**Not changed in this PR:** ${unchangedList}`;
            }

            if (dataSection) {
              detailedReport += dataSection + '\n\n---\n\n';
            }

            if (credentialIssues > 0) {
              detailedReport += `### 🔐 Credential Scan Issues

            Review the "Check for credentials" step to see what was detected.

            **How to fix:**
            - Remove sensitive files from the commit
            - Use environment variables: \`Sys.getenv("API_KEY")\`
            - Add sensitive files to \`.gitignore\`

            ---

            `;
            }

            if (structureIssues > 0) {
              detailedReport += `### 📦 Package Structure Issues

            Review the "Check R package structure" step in the workflow logs.

            **How to fix:**
            - Ensure standard R package structure
            - Run \`devtools::check()\` locally

            ---

            `;
            }

            if (testStatus === 'failed') {
              detailedReport += `### 🧪 Test Failures

            Review the "Run testthat tests" step in the workflow logs.

            **How to fix:**
            - Run tests locally: \`devtools::test()\`
            - Fix failing tests

            ---

            `;
            }

            if (updateType !== 'biweekly' && newOpenErrors > 0) {
              detailedReport += `### 🧬 New QC Issues vs main (\`data-raw/qc/qc_log_errors.csv\`)

            This branch has **${newOpenErrors} new** error-level issue(s) marked \`open\` that were not already open on main (on top of ${totalOpenErrors - newOpenErrors} pre-existing open error(s) already on this branch, which are not blocking this PR).

            These were rendered by update-data.yml's "Run annual QC + stream team review" step and already committed to this branch - check that workflow run's log for details on what triggered each one.

            **How to resolve:**
            - Open \`data-raw/qc/qc_log_errors.csv\` on this branch and find the rows with \`status = open\` that aren't open on main (see the workflow log above for their \`log_id\`s)
            - For each: set \`status\` to \`reviewed_no_issue\`, \`fixed_in_patch\` (and add a fix block to \`data-raw/qc/fixes/\`), or \`fixed_in_source\`, with \`reviewer_notes\` explaining the call
            - Commit the updated log to this branch and push - this check re-runs and only re-fails on issues still marked \`open\` and not open on main

            ---

            `;
            }

            // ==========================================
            // ANNUAL QC SUMMARY REPORT LINK
            // ==========================================
            // update-data.yml already rendered data-raw/qc/qc_summary.qmd (via
            // run_annual_qc.R) and committed the output HTML to this branch,
            // for every non-biweekly update - this just links to the newest
            // one rather than re-rendering anything.
            let qcSummarySection = '';
            const qcReportsDir = 'data-raw/qc/reports';
            if (fs.existsSync(qcReportsDir)) {
              const qcSummaryFiles = fs.readdirSync(qcReportsDir)
                .filter(f => /^qc_summary_\d{4}-\d{2}-\d{2}\.html$/.test(f))
                .sort();

              if (qcSummaryFiles.length > 0) {
                const latestSummary = qcSummaryFiles[qcSummaryFiles.length - 1];
                const runDate = latestSummary.match(/(\d{4}-\d{2}-\d{2})/)[1];

                const siblingReports = [
                  { label: 'RST', file: `rst_qc_${runDate}.html` },
                  { label: 'Flow & Temperature', file: `flow_qc_${runDate}.html` },
                  { label: 'Efficiency Trials', file: `efficiency_qc_${runDate}.html` },
                ].filter(r => fs.existsSync(`${qcReportsDir}/${r.file}`));

                qcSummarySection = `### 📈 Annual QC Summary\n\n` +
                  `[Open the QC summary report (run ${runDate})](${previewLink(`${qcReportsDir}/${latestSummary}`)}) — ` +
                  `this run year vs. historical average across every check, rendered from \`data-raw/qc/qc_summary.qmd\`.\n`;

                if (siblingReports.length > 0) {
                  qcSummarySection += `\nSame run, per-topic detail: ` +
                    siblingReports.map(r => `[${r.label}](${previewLink(`${qcReportsDir}/${r.file}`)})`).join(' · ') +
                    `\n`;
                }
                qcSummarySection += `\n---\n\n`;
              }
            }

            // ==========================================
            // STREAM TEAM REPORT LINKS
            // ==========================================
            // update-data.yml already rendered render_reviews.R and committed
            // one report per stream to this branch, for every non-biweekly
            // update - this just links to what's already there.
            let streamTeamSection = '';
            const streamReportsDir = 'data-raw/data-checks/stream_team_review/stream_team_reports';
            if (fs.existsSync(streamReportsDir)) {
              const streamLinks = fs.readdirSync(streamReportsDir, { withFileTypes: true })
                .filter(d => d.isDirectory())
                .map(d => {
                  const htmlFile = fs.readdirSync(`${streamReportsDir}/${d.name}`).find(f => f.endsWith('.html'));
                  if (!htmlFile) return null;
                  const label = d.name.replace(/_/g, ' ').replace(/\b\w/g, c => c.toUpperCase());
                  return `- [${label}](${previewLink(`${streamReportsDir}/${d.name}/${htmlFile}`)})`;
                })
                .filter(Boolean)
                .sort();

              if (streamLinks.length > 0) {
                streamTeamSection = `### 🎣 Stream Team Reports\n\n` +
                  `Rendered by \`render_reviews.R\`, one per stream (Sacramento River split by site):\n\n` +
                  streamLinks.join('\n') + `\n\n---\n\n`;
              }
            }

            detailedReport += qcSummarySection + streamTeamSection;

            const infoFooter = `
            <details>
            <summary>ℹ️ About these checks</summary>

            - **Credential Scan:** Prevents accidental commit of secrets
            - **Package Structure:** Ensures R package standards
            - **Tests:** Verifies code functionality
            - **Data Comparison (vs main):** Compares every changed .rda file against main and reports what changed; purely informational, does not fail this check or require approval — a structural problem (a column disappearing, a load error, a data type change) is still called out in the table but even that no longer blocks the PR here (see \`inst/scripts/compare_data_files.R\`)
            - **QC Log (vs main):** Compares this branch's already-committed \`data-raw/qc/qc_log_errors.csv\` (rendered by update-data.yml) against main's copy; only fails on *newly* open error-level issues not already open on main (skipped entirely for biweekly updates)
            - **Annual QC Summary / Stream Team Reports:** Links to the HTML reports update-data.yml already rendered and committed to this branch; nothing here is regenerated by this check

            </details>`;

            const fullComment = statusSummary + detailedReport + infoFooter;

            const comments = await github.rest.issues.listComments({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
            });

            const botComment = comments.data.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('Quality Check Summary')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: fullComment
              });
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: fullComment
              });
            }

      - name: Final status check
        if: always()
        run: |
          echo "=== Final Status Check ==="

          CREDENTIAL_ISSUES="${{ steps.credential-check.outputs.credential_issues }}"
          STRUCTURE_ISSUES="${{ steps.structure-check.outputs.structure_issues }}"
          TEST_STATUS="${{ steps.testthat.outputs.test_status }}"
          COMPARISON_EXIT="${{ steps.comparison.outputs.comparison_exit_code }}"
          UPDATE_TYPE="${{ steps.update-type.outputs.update_type }}"
          NEW_OPEN_ERRORS="${{ steps.qc-comparison.outputs.new_open_errors }}"

          if [ -z "$CREDENTIAL_ISSUES" ]; then CREDENTIAL_ISSUES=0; fi
          if [ -z "$STRUCTURE_ISSUES" ]; then STRUCTURE_ISSUES=0; fi
          if [ -z "$TEST_STATUS" ]; then TEST_STATUS=unknown; fi
          if [ -z "$COMPARISON_EXIT" ]; then COMPARISON_EXIT=0; fi
          if [ -z "$NEW_OPEN_ERRORS" ]; then NEW_OPEN_ERRORS=0; fi

          echo "Credential issues: $CREDENTIAL_ISSUES"
          echo "Structure issues: $STRUCTURE_ISSUES"
          echo "Test status: $TEST_STATUS"
          echo "Comparison exit code (informational only - see PR comment): $COMPARISON_EXIT"
          echo "Update type: $UPDATE_TYPE"
          echo "New open QC errors vs main: $NEW_OPEN_ERRORS"
          echo ""

          FAILURES=0

          if [ "$CREDENTIAL_ISSUES" != "0" ]; then
            echo "❌ Credential scan found issues"
            FAILURES=$((FAILURES + 1))
          fi

          if [ "$STRUCTURE_ISSUES" != "0" ]; then
            echo "❌ Package structure has issues"
            FAILURES=$((FAILURES + 1))
          fi

          if [ "$TEST_STATUS" = "failed" ]; then
            echo "❌ Tests failed"
            FAILURES=$((FAILURES + 1))
          fi

          # Data file comparison is informational only (see the PR comment's
          # Data File Comparison section) and never contributes to FAILURES -
          # changed values, and even a structural issue like a removed
          # column, no longer block the PR here.

          if [ "$UPDATE_TYPE" != "biweekly" ] && [ "$NEW_OPEN_ERRORS" != "0" ]; then
            echo "❌ QC log has $NEW_OPEN_ERRORS new open error(s) vs main - see data-raw/qc/qc_log_errors.csv"
            FAILURES=$((FAILURES + 1))
          fi

          echo ""
          if [ "$FAILURES" -eq 0 ]; then
            echo "✅ All checks passed!"
            exit 0
          else
            echo "❌ $FAILURES check(s) failed"
            echo "Review the PR comment for details."
            exit 1
          fi
